</head>
<h1>Listing Registered users</h1>
<table id= "tab" class = "table table-striped">
  <tr>
    <th>Name</th>
    <th>Email_id</th> 
    <th></th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
    <% if user.id == current_user.id %>
      <% next %>
    <% end %>
    <% i=0 %>
    <tr>  
      <td><%= user.name %>&nbsp;&nbsp;&nbsp;&nbsp;</td>
      <td><%= user.email %></td>
      <% current_user.followers.each do |follower| %>
        <% if check_for_follow_unfollow_link?(follower,user) %>
          <td><%= button_to 'Unfollow', follower, method: :delete, data: { confirm: 'are you sure?' } , :class => "btn btn-mini btn-primary"%></td>
          <% i=1 %> 
          <% break %>
        <% else %>
          <% i=0 %> 
        <% end %> 
      <% end %>   
      <% if i==0 %>
        <%= form_for(@follower) do |f| %>
          <% if @follower.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@follower.errors.count, "error") %> prohibited this follower from being saved:</h2>
              <ul>
                <% @follower.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <%= f.hidden_field(:user_id, :value => current_user.id) %>
          <%= f.hidden_field(:follower_id, :value => user.id) %>
          <div class="actions">
            <td><%= f.submit "Follow", :class => "btn btn-mini btn-success" %></td>
          </div>
        <% end %>
      <% end %>
    </tr>
  <% end %>
</table>
Total Registers Users <%= @users.count %><br/><br/>
<%= link_to 'View followers', followers_path, :class => "btn btn-mini btn-primary" %>
